export const logger = store => next => action =>  {
    // console.group(action.type);
    // console.info('dispatching', action);
    // let result = next(action);
    // console.log('next state', stoe.getStore());
    // console.groupEnd();
    // return result;
};

